package pt.estga.bookmark.enums;

/**
 * Types of bookmark targets supported by the application.
 * This enum is intentionally small and explicit to keep mapping and routing predictable.
 */
public enum BookmarkTargetType {
    MONUMENT,
    MARK,
    MARK_OCCURRENCE,
    MARK_EVIDENCE,
    UNKNOWN
}
